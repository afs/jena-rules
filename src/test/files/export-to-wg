#!/usr/bin/bash

hDIR="$HOME/W3C/SHACL/rules-shacl-wg/shacl12-test-suite/tests/rules/"
##DIR="$HOME/W3C/SHACL/shacl-wg/shacl12-test-suite/tests/rules/"

function exportDir {
    local SRC="$1"
    rm -rf "$DIR/$1"
    cp -r $SRC $DIR
}

function exportArea {
    local AREA="$1"
    rm -rf "$DIR/$AREA"
    exportDir "$AREA"
}

## exportArea syntax
## exportArea wellformed
## exportArea stratification
## exportArea eval


## rm -f $DIR/README.md
## rm -f $DIR/manifest*
##cp manifest-rules.ttl $DIR
##cp README.md $DIR

